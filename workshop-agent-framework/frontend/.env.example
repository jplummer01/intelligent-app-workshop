# Environment variables for the frontend application

# API_URL: Used by Node.js server.js at runtime to proxy requests to the backend API
# For local development, use localhost. When deployed, this is set via Container Apps env vars.
API_URL=http://localhost:5000/chat

# REACT_APP_PROXY_URL: Baked into React build at build time
# Use relative path /api/chat so it works both locally and when deployed
REACT_APP_PROXY_URL=/api/chat

# PORT: Port where the Node.js server runs
PORT=80
